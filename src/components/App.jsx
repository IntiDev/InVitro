import React from 'react';
import Button from './Button';

const App = () => {
  return (
    <div>
      <Button text="Enviar" />
      <Button text="Enviar" disabled />
    </div>
  );
};

export default App;
